{
  "scripts": {
    "test": "elm make --docs=documentation.json && (cd examples && elm make Examples.elm) && elm-format --validate ."
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-format": "^0.8.0"
  }
}
