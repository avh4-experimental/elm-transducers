{
  "scripts": {
    "test": "elm-test && elm make --docs=documentation.json && (cd examples && elm make --debug Examples.elm) && elm-format --validate ."
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-format": "^0.8.0",
    "elm-test": "^0.19.0-beta5"
  }
}
